 declare module 'marked' {
        // Define the types for the functions and properties you use from marked here
        // For example:
        function marked(text: string, options?: { [key: string]: any }): string;
        // Add other definitions as needed
    }